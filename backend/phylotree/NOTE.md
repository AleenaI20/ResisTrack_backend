# Offline Track B pipeline preserved from origin/phylotree
# Use the FastAPI endpoint for interactive tree building:
#   POST /api/phylogeny/build
# Run this offline pipeline for BV-BRC training-set generation:
#   cd phylotree && python -m src.pipeline --all
